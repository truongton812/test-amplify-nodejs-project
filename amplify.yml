version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm install  # Đảm bảo deps
        - echo "Build Node.js server complete"
  artifacts:
    baseDirectory: /  # Hoặc '.' thay vì 'dist'
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
backend:
  phases:
    build:
      commands:
        - echo "No backend"

